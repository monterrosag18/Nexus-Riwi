// TACTICAL UI STRINGS & TEMPLATES (Refactored from Components)

export const LOGIN_TEMPLATE = (state) => `
    <div class="flex w-full h-full m-0 p-0">
        <!-- Refactored Login UI -->
        <div class="flex w-full h-full items-center justify-center relative circuit-wrapper">
             <!-- ... truncated for brevity ... -->
        </div>
    </div>
`;

export const SHOP_TEMPLATE = (user, clanData) => `
    <div id="shop-ui-layer" style="position:relative; z-index:10;">
        <!-- Refactored Shop UI -->
    </div>
`;

export const PROFILE_MODAL_TEMPLATE = (user, clanData) => `
    <!-- Template for Profile Panel -->
`;
